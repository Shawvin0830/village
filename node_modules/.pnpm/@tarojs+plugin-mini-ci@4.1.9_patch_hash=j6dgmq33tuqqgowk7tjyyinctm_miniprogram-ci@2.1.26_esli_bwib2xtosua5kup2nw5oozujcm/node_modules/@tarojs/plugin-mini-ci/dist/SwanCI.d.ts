import BaseCI from './BaseCi';
export default class SwanCI extends BaseCI {
    private swanBin;
    init(): void;
    open(): Promise<void>;
    preview(): Promise<void>;
    upload(): Promise<void>;
}
